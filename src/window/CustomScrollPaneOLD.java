package window;

import enums.TypeOfScrollPaneOLD;

/** Legacy stub — replaced by CustomScrollPane. */
public class CustomScrollPaneOLD extends CustomScrollPane {
    public CustomScrollPaneOLD(TypeOfScrollPaneOLD type, double w, double h) {
        super(enums.TypeOfScrollPane.VERTICALLY_RIGHT, w, h);
    }
}
